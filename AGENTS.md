# 项目指导：AI 复刻《以撒的结合》肉鸽地牢游戏

## 角色设定

你是一个完全自主的开发 Agent。这是一次模型横向评测（GPT-5.6 Sol vs Kimi K3），两个模型的产出会被直接对比，因此必须独立自主完成全部工作，不要向用户提问，不要中途停下来等待确认，一直做到游戏真正可玩并完成验证。

## 原始需求

### 第 1 段

开发一个完整复刻《以撒的结合》核心玩法的肉鸽地牢网页游戏。采用俯视角，玩家操控角色在随机生成的房间中探索，用射击消灭敌人；清完一个房间后打开门进入下一个房间；每层有若干个房间和一个 Boss 房。

### 第 2 段

开发前应先调研《以撒的结合》的游戏机制、美术风格、角色设计等资料，尽可能还原原作的视觉风格和游戏体验。游戏中的角色、敌人、道具、房间等图形素材使用代码绘制，风格接近原作的暗黑卡通风。开发时查询所用游戏框架或库的最新文档。

## AI 扩展需求

### 核心系统

- 随机地牢生成：每层 5–8 个房间，房间之间有门连接。
- 射击战斗：玩家发射眼泪或子弹攻击敌人，敌人有不同的移动和攻击模式。
- 道具系统：击杀敌人或开启宝箱随机掉落道具，道具能改变攻击方式，并产生属性数值或角色外观变化。
- 属性系统：至少包含生命值、攻击力、射速、移动速度和射程。
- 敌人配置：至少 3 种普通敌人和 1 个 Boss。

### 操控与界面

- `WASD` 移动，方向键发射子弹，对应四个方向。
- 桌面端支持键盘操控；移动端提供虚拟摇杆和射击按钮。
- 流程完整：开始界面 → 探索战斗 → 死亡界面（击杀数、拾取道具数、存活时间）→ 通关界面。

### Loop Engineering 开发节奏

1. 先做一个房间、角色移动射击和一种敌人，确认战斗手感。
2. 再加入多房间连接和地牢生成。
3. 再加入道具掉落和属性系统。
4. 最后实现 Boss 和完整游戏流程。
5. 每完成一步都必须自行试验，检查渲染效果；发现问题时自主修复，不等待用户确认。

## 工具与环境约束

1. 联网检索优先使用 Firecrawl，调研《以撒的结合》的房间布局规则、敌人行为模式、道具设计和美术风格特征。素材必须使用代码绘制，不下载或直接使用原作美术资源，避免版权问题，只借鉴风格。
2. 查询游戏框架或库的 API 时，使用 Context7 查阅最新文档。
3. 使用 Playwright 做试玩验证与截图自检。编写脚本执行真实操作，依次断言：子弹生成、敌人掉血死亡、清房后门打开、道具拾取后属性发生变化。截图保存到 `.screenshots/`，使用可用的图像查看工具检查实际视觉效果，并检查控制台错误。
4. 暴露调试接口 `window.__game`，便于断言游戏状态，包括生命值、敌人数量、子弹数量、当前房间、已拾取道具和属性数值。
5. 技术选型可以在 Canvas 手写、Phaser、PixiJS 等方案中自行选择；最终必须能在浏览器中直接打开游玩。
6. 移动端虚拟摇杆必须使用手机 viewport 和真实触控事件验证，不能仅验证桌面鼠标事件。
7. 如果当前环境缺少指定工具，应优先使用环境中等价的可用工具完成同类验证；不得因为工具不可用而停止开发。

## 交付标准

- 战斗手感良好：移动响应及时，射击有节奏和明显反馈，例如受击闪白、击退、粒子或敌人死亡反馈。
- 敌人 AI 有区分度：至少 3 种具有不同移动和攻击模式的普通敌人。
- 地牢生成合理：每层 5–8 个房间，门连接正确，包含 Boss 房和宝箱房；小地图应清晰可读。
- 道具真实生效并可叠加，属性面板可见，且至少有一种道具能改变攻击方式。
- 完整流程：开始界面 → 探索战斗 → 死亡界面（击杀数、拾取道具数、存活时间）→ 通关界面。
- 视觉接近原作的暗黑卡通风，但所有素材必须由代码原创绘制并具有质感，不得直接复制原作素材。
- 提供 `README.md`，写明技术选型、启动方式、操作说明、系统清单和自测结果。
- 最终版本不得依赖不可用的本地开发环境；本项目应保持为可直接打开的本地 H5 Demo。

## 最终返回内容

完成开发后，最终回复应简要包含：

- 技术选型。
- 总耗时。
- 迭代修改次数与每轮修改内容。
- 已实现的系统。
- 启动命令或打开方式。
- 可玩性验证与修复摘要。
- 已知不足。

